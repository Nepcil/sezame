{% extends 'base.html.twig' %}

{% block title %}Book{% endblock %}

{% block body %}
<style>

</style>
    {% if books | length > 0 %}
        {% for book in books %}
            <div class="example-wrapper">
                <h1>{{ book.title }}</h1>
                <p>{{ book.summary }}</p>
                <p>{{ book.price }} €</p>
                <p>{{ book.ranking }}</p>
            </div>
        {% endfor %}
    {% endif %}
{% endblock %}
