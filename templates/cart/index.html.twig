{% extends 'base.html.twig' %}

{% block title %}Panier{% endblock %}

{% block body %}

    <h1>Dans votre panier</h1>

    

        <table class="table">
            <tr>
                <td class="tableTitle">Titre</td>
                <td class="tableTitle">Price en €</td>
                <td class="tableTitle">Quantité</td>
                <td class="tableTitle">ISBN</td>
                <td class="tableTitle">Image</td>
                <td class="tableTitle">Total en €</td>
            </tr>
            <tr>

    {% if cart | length > 0  %} 

            {% for cart in carts %}
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
                <td></td>
            {% endfor %}

            
            </tr>
            <tr>
                <td class="totalPrice" colspan="6">€</td>
            </tr>
        </table>
    {% endif %}
    {% if cart | length < 0 %}
        <div class="buttonSubmit">
            <button class="resetSubmit">supprimer</button>
            <button class="totalSubmit">payer</button>
        </div>
    {% else %}
        <div class="cadreEmpty">
            <span class="emptyP cart">
                Votre panier est vide 
            </span>
        </div>
    {% endif %}

{% endblock %}

